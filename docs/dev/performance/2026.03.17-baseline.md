---
작성일시: 2026-03-17
도메인: Performance (성능)
목적: WIG 주요 API의 기준선 성능 지표를 기록하고, 최적화 전/후 비교 기준을 고정한다.
---

# WIG 성능 Baseline (1차)

## 1. 측정 목적

- 최적화의 효과를 체감이 아닌 수치로 비교하기 위한 기준선 문서
- 이후 변경은 동일 조건에서 재측정하여 p50/p95 개선 여부를 기록

## 2. 측정 범위

- 우선 엔드포인트
  - `GET /api/dashboard/team`
  - `GET /api/scoreboards/:id/logs/weekly`
  - `GET /api/scoreboards/:id/logs/monthly`
- 환경
  - Cloudflare Workers + D1 (운영과 동일한 런타임 기준)
- 측정 단위
  - CPU Time, Wall Time, Duration, Errors

## 3. 초기 기준선 (2026-03-17)

> 출처: Cloudflare 대시보드 스냅샷

| 메트릭 | 값 | 비고 |
|---|---:|---|
| CPU Time | 86ms | 목표 대비 높음 |
| Wall Time | 306ms | 느림 구간 |
| Duration | 305ms | 사용자 지연 체감 가능 |
| Errors | 0 | 안정적 |

## 4. 목표치 (1차)

| 메트릭 | 현재 | 1차 목표 | 2차 목표 |
|---|---:|---:|---:|
| CPU Time | 86ms | <= 40ms | <= 25ms |
| Duration | 305ms | <= 220ms | <= 180ms |
| Errors | 0 | 0 유지 | 0 유지 |

## 5. 측정 템플릿

아래 표를 반복 추가해 최적화 단위별로 기록한다.

### 측정 기록 템플릿

- 측정일:
- 대상 변경:
- 측정 엔드포인트:
- 데이터 규모(멤버 수/지표 수):
- 샘플 수:

| 지표 | p50 | p95 | 비고 |
|---|---:|---:|---|
| CPU Time |  |  |  |
| Wall Time |  |  |  |
| Duration |  |  |  |
| Errors |  |  |  |

## 6. 기록 규칙

- 한 번에 하나의 최적화 묶음만 적용하고 측정한다.
- p50/p95 모두 개선되지 않으면 롤백 또는 추가 원인 분석을 수행한다.
- 엔드포인트별 편차가 큰 경우 데이터 규모를 분리해 별도 표로 기록한다.

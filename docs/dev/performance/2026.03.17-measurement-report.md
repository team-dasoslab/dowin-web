---
작성일시: 2026-03-17
도메인: Performance (성능)
목적: 엔드포인트별 30회 샘플 측정 결과를 표준 포맷으로 기록하고, 최적화 전/후 비교 데이터를 누적한다.
---

# WIG 성능 측정 리포트 (Run #1)

## 1. 측정 요약

- 측정일: 2026-03-17
- 측정 환경: Cloudflare Workers + D1 (운영과 동일 런타임)
- 샘플 기준: 엔드포인트별 30회 이상
- 비교 기준 문서:
  - `docs/dev/performance/2026.03.17-optimization-points.md`
  - `docs/dev/performance/2026.03.17-baseline.md`

## 2. 측정 대상

1. `GET /api/dashboard/team`
2. `GET /api/scoreboards/:id/logs/weekly`
3. `GET /api/scoreboards/:id/logs/monthly`

## 3. 실행 절차

### 3.0. 빠른 입력 체크리스트 (복붙용)

```text
[Run # ]
date:
endpoint:
sample_count: 30
dataset: members= / active_measures= / logs=
cpu_ms: p50= , p95=
wall_ms: p50= , p95=
duration_ms: p50= , p95=
errors: count= , rate=
change_summary:
```

### 3.1. 요청 조건 고정

- 동일 사용자 세션으로 요청
- 동일 워크스페이스/점수판 데이터셋 사용
- 워밍업 5회 이후 본 측정 30회 수행

### 3.2. 기록 규칙

- Cloudflare 대시보드 또는 동일 출처의 지표만 사용
- CPU Time, Wall Time, Duration은 p50/p95를 모두 기록
- Errors는 총 요청 대비 오류 개수와 비율을 함께 기록

## 4. Run #1 결과

### 4.1. `GET /api/dashboard/team`

- 데이터 규모(예시 기록): 멤버 ___명 / ACTIVE 선행지표 ___개
- 샘플 수: 30

| 지표 | p50 | p95 | 비고 |
|---|---:|---:|---|
| CPU Time |  |  |  |
| Wall Time |  |  |  |
| Duration |  |  |  |
| Errors |  |  |  |

### 4.2. `GET /api/scoreboards/:id/logs/weekly`

- 데이터 규모(예시 기록): 선행지표 ___개 / 주간 로그 ___건
- 샘플 수: 30

| 지표 | p50 | p95 | 비고 |
|---|---:|---:|---|
| CPU Time |  |  |  |
| Wall Time |  |  |  |
| Duration |  |  |  |
| Errors |  |  |  |

### 4.3. `GET /api/scoreboards/:id/logs/monthly`

- 데이터 규모(예시 기록): 선행지표 ___개 / 월간 로그 ___건
- 샘플 수: 30

| 지표 | p50 | p95 | 비고 |
|---|---:|---:|---|
| CPU Time |  |  |  |
| Wall Time |  |  |  |
| Duration |  |  |  |
| Errors |  |  |  |

## 5. 글로벌 기준선 스냅샷(참고)

> 엔드포인트 분리 전 Cloudflare 스냅샷 (2026-03-17)

| 메트릭 | 값 |
|---|---:|
| CPU Time | 86ms |
| Wall Time | 306ms |
| Duration | 305ms |
| Errors | 0 |

## 6. 해석 메모

- 이번 Run에서 가장 높은 p95 Duration 엔드포인트를 P0 최적화 대상으로 지정한다.
- CPU Time과 Duration이 함께 높은 경우, 애플리케이션 집계 로직 최적화를 우선한다.
- Duration만 높은 경우, DB 조회 범위/쿼리 패턴과 I/O 대기를 우선 점검한다.

## 7. 다음 업데이트 규칙

- 최적화 적용 후 같은 문서에 `Run #2`, `Run #3` 섹션을 추가한다.
- 각 Run은 "변경 내용 1줄 요약 + 동일 포맷 표"를 유지한다.

---
작성일시: 2026-03-14
도메인: Workspace (워크스페이스)
목적: 워크스페이스 도메인 프론트엔드 구현 결과 및 가이드를 기록한다.
---

# Workspace 프론트엔드 구현 결과

## 1. 구현 개요

사용자가 워크스페이스에 소속되어 있지 않은 경우를 처리하기 위한 UI 작업을 완료했습니다. API 연결을 통해 실시간으로 상태를 확인하며, 직관적인 워크스페이스 생성 흐름을 제공합니다.

## 2. 주요 작업 내역

### 2.1. 대시보드 워크스페이스 체크 (`MyDashboardPage`)
- `useGetWorkspacesMe` 훅을 통합하여 실시간 워크스페이스 상태 조회.
- **No Workspace 상태 (404)**: 사용자가 제안한 레이아웃을 바탕으로 "소속된 워크스페이스가 없어요" CTA 화면 구현.
- **로딩 상태 처리**: API 호출 중 스켈레톤 또는 빈 화면 처리 (`null` 반환).

### 2.2. 워크스페이스 생성 페이지 (`/workspace/new`)
- 신규 워크스페이스를 생성할 수 있는 전용 페이지 구현.
- `usePostWorkspaces` 뮤테이션을 사용하여 백엔드와 연동.
- 생성 성공 시 대시보드로 자동 이동.

## 3. 사용된 기술 및 패턴
- **Orval Hooks**: `useGetWorkspacesMe`, `usePostWorkspaces` 등 자동 생성된 TanStack Query 훅 사용.
- **Lucide React**: `Zap`, `Plus`, `ArrowLeft` 아이콘 사용.
- **Tailwind CSS**: 일관된 디자인 시스템 유지를 위한 유틸리티 클래스 활용.

## 4. 검증 완료
- `tsc --noEmit`: 타입 체크 통과.
- `workspaceResponse`의 `status`를 기반으로 한 조건부 렌더링 정상 동작 확인.

---

## 5. 향후 과제
- 워크스페이스 설정(이름 수정) UI.
- 워크스페이스 멤버 초대 링크 공유 기능.
- 대시보드 내의 나머지 Mock 데이터를 실제 API로 점진적 교체.

---
작성일시: 2026-03-09 01:02:08
목적: "가중목(WIG)" 관리 서비스의 전체적인 컨셉, 데이터 구조, UI 흐름을 하나의 문서에서 통합 관리한다.
---

# 가중목 서비스 기획 개요

## 1. 배경 및 맥락 (Background & Context)

- **배경**: 현재 지인들과 가중목(WIG)을 관리하며 주 1회 회의를 하고 있음. 노션(Notion)을 통해 기록하고 있으나, 시스템의 능동성이 부족함.
- **문제**: 노션은 직접 찾아가서 채워야 하는 구조라, 일상에 쫓기다 보면 **모임 직전에 다급하게 점수판을 채워넣는** 나를 발견하게 됨. 목표가 단지 기록을 위한 기록으로 전락하고, 스스로 동기부여가 되지 않아 관성적으로 참여하고 있음.

## 2. 우선순위 및 당위성 (Priority & Rationale)

- **왜 지금 해야 하는가?**: 단순히 '기록'하는 수준을 넘어, 4DX를 제대로 활용하여 삶의 실질적인 변화를 만들어내고 싶음. 스스로 동기부여가 되지 않는 현재의 '흐지부지한' 상태를 혁파해야 함.
- **다른 것이 아닌 이것을 왜 해야 하는가?**: 범용적인 도구(노션)는 4DX의 핵심 철학인 '집중'과 '실시간 가시성'을 보장해주지 못함. 내가 매일 기분 좋게 들여다보고, 나를 자극하는 전용 시스템이 필요함.

## 3. 기대 효과 (Impact: Before & After)

- **이전**: 모임 직전 숙제하듯 점수판을 채움. 동기부여가 안 되어 목표가 표류함. 지인들 틈에 섞여 관성적으로 흘러가는 느낌.
- **이후**: '항상 옆에 있는 점수판'을 통해 매일 나의 진척도를 즐겁게 확인. 억지로 채우는 기록이 아닌, 나 스스로의 승리를 확인하는 과정으로 변화하여 내적인 몰입도가 극대화됨.

## 4. 상세 설계 (Detailed Design)

### 4.1. 데이터 모델 (Data Model)

- **User (사용자)**: 닉네임과 수동 ID 기반의 인증 주체.
- **Workspace (워크스페이스)**: 팀원들이 모이는 전용 공간 (MVP는 1인 1워크스페이스).
- **Scoreboard (점수판)**: `목표 (WIG) > 후행지표 (Lag) > 선행지표 (Lead)`의 집합체.
- **Daily Log (일일 기록)**: 선행지표에 대한 요일별 O/X 성취 기록.

### 4.2. UI/UX 흐름 (UI/UX Flow)

- **Dashboard**: "이번 주 나의 승패"를 직관적으로 노출. WIG 게이지와 이번 주 선행지표 현황을 한 줄로 요약.
- **Meeting Mode**: 15분 회의를 위한 공약 체크, 결과 확인, 차주 공약 입력이 한 화면에서 이루어지는 흐름. 팀 단위로 사용 시 돌아가며 보고할 수 있는 뷰 제공.

## 5. 구체적 실행 방안 (Action Items)

### 기획 및 설계 (MVP 중심)

- [x] **[브랜딩]** 서비스명 확정: **WIG**
- [x] **[기획]** 기능 명세 기반 데이터베이스 테이블 구조(D1) 설계
- [x] **[디자인]** MVP 스펙 중심의 모바일 반응형 UI 설계 및 프로토타입 구축
- [ ] **[기획]** 위젯용 통계 집계 쿼리 및 로직 정의

### 구현 현황 업데이트 (2026-03-15)

- [x] **[백엔드]** Daily Log API 구현 완료
  - `GET /api/scoreboards/:id/logs/weekly`
  - `PUT /api/lead-measures/:id/logs/:date`
  - `DELETE /api/lead-measures/:id/logs/:date`
- [x] **[프론트엔드]** `dashboard/my` 화면을 실제 API 데이터로 전환
  - 활성 워크스페이스, 활성 점수판, 주간 로그를 Orval + TanStack Query로 조회
  - 주간 점수판 셀 클릭 시 `미기록 -> O -> X -> 미기록` 순환 토글 적용
  - 미래 날짜 비활성화, 로딩/빈 상태 분기 유지
- [x] **[프론트엔드]** Daily Log 낙관적 업데이트 및 실패 롤백 적용
- [x] **[기술부채]** Next App Router 동적 세그먼트 충돌 정리
  - `lead-measures/[leadMeasureId]` 계열을 `lead-measures/[id]`로 통일
  - `scoreboards/[scoreboardId]` 계열을 `scoreboards/[id]`로 통일
- [x] **[기술부채]** `getSession(db)` 타입 정의를 실제 Drizzle DB 타입 기준으로 보정

### 다음 액션

- [ ] **[프론트엔드]** `dashboard/my`의 confetti 축하 인터랙션 연결
- [ ] **[프론트엔드]** `measure/[id]`, `dashboard` 팀 뷰 등 남은 mock 의존 화면 API 연동
- [ ] **[품질]** 저장소 전역 TypeScript 오류와 `yarn lint` 스크립트 설정 문제 정리
- [ ] **[문서]** 도메인별 구현 문서와 품질 기준 문서에 현재 검증 상태 반영

## 6. 예상 비용 및 기간 (Cost & Estimation)

- **기간**: 기획 완료 후 MVP 개발까지 약 2~3주 예상
- **비용**: 1인 개발/운영 기준 Cloudflare 프리티어 범위 내에서 소화 가능.

## 7. 성공 및 실패 판단 기준 (Success & Failure Criteria)

- **성공**: 지인들과의 15분 가중목 회의 시, 스프레드시트 대신 이 서비스만 사용하여 보고와 계획이 완결될 때.
- **실패**: 점수판을 보고도 현재 내가 잘하고 있는지(Winning) 즉각적으로 파악되지 않을 때.

## 8. 구현 메모 (2026-03-15)

- `dashboard/my`는 현재 프로토타입 UI를 유지한 채 실제 세션 쿠키 기반 API를 사용한다.
- 사용자 닉네임 표시는 기존 로그인 프로토타입의 `localStorage` 값을 임시로 재사용한다.
- 변경 파일 기준 ESLint는 통과했다.
- 저장소 전체 `yarn tsc --noEmit`는 이번 작업과 별개인 기존 타입 오류로 아직 통과하지 않는다.

## 9. 다음 단계 기획을 위해 필요한 정보 (Information Needed)

더 구체적인 설계를 위해 아래 정보들이 필요합니다.

### 9.1. 서비스 아이덴티티 (Identity)

- **가상의 서비스 이름**: (예: WIG-Master, WinLog, Focus-Board 등)
- **추구하는 톤앤매너**: (예: 진지하고 강력한 동기부여 vs 깔끔하고 모던한 대시보드 vs 가벼운 게임 같은 느낌)

### 9.2. 데이터 및 지표 상세 (Data Specs)

- **현재 사용 중인 노션/스프레드시트 샘플**: 어떤 지표(숫자, 시간, 완료여부 등)를 주로 관리하시는지에 대한 구체적인 예시.
- **지표의 복잡도**: 한 명의 사용자가 보통 몇 개의 선행지표를 관리하는지?

### 9.3. 상호작용 및 알림 (Interaction & Notification)

- **기록 리마인드**: 하루 중 언제 기록을 상기시켜주는 것이 가장 효과적일지?
- **팀 공유 노출도**: 지인들에게 내 어떤 데이터까지 공개하고 싶으신지? (단순 승패만 vs 구체적인 선행지표 달성 수치까지)

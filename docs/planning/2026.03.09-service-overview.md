---
작성일시: 2026-03-09 01:02:08
목적: "가중목(WIG)" 관리 서비스의 전체적인 컨셉, 데이터 구조, UI 흐름을 하나의 문서에서 통합 관리한다.
---

# 가중목 서비스 기획 개요

> 상태 메모 (2026-03-30): 이 문서는 제품 기획 스레드를 누적 관리한 문서다. 현재 구현 기준으로는 공개 회원가입, 복원코드 기반 계정 복구, 푸시 알림, 팀 메모 레일이 이미 반영되어 있으며, 일부 `폐쇄형 운영` 전제와 초기 액션 아이템은 히스토리 맥락으로 읽어야 한다.

## 1. 배경 및 맥락 (Background & Context)

- **배경**: 현재 지인들과 가중목(WIG)을 관리하며 주 1회 회의를 하고 있음. 노션(Notion)을 통해 기록하고 있으나, 시스템의 능동성이 부족함.
- **문제**: 노션은 직접 찾아가서 채워야 하는 구조라, 일상에 쫓기다 보면 **모임 직전에 다급하게 점수판을 채워넣는** 나를 발견하게 됨. 목표가 단지 기록을 위한 기록으로 전락하고, 스스로 동기부여가 되지 않아 관성적으로 참여하고 있음.

## 2. 우선순위 및 당위성 (Priority & Rationale)

- **왜 지금 해야 하는가?**: 단순히 '기록'하는 수준을 넘어, 4DX를 제대로 활용하여 삶의 실질적인 변화를 만들어내고 싶음. 스스로 동기부여가 되지 않는 현재의 '흐지부지한' 상태를 혁파해야 함.
- **다른 것이 아닌 이것을 왜 해야 하는가?**: 범용적인 도구(노션)는 4DX의 핵심 철학인 '집중'과 '실시간 가시성'을 보장해주지 못함. 내가 매일 기분 좋게 들여다보고, 나를 자극하는 전용 시스템이 필요함.

## 3. 기대 효과 (Impact: Before & After)

- **이전**: 모임 직전 숙제하듯 점수판을 채움. 동기부여가 안 되어 목표가 표류함. 지인들 틈에 섞여 관성적으로 흘러가는 느낌.
- **이후**: '항상 옆에 있는 점수판'을 통해 매일 나의 진척도를 즐겁게 확인. 억지로 채우는 기록이 아닌, 나 스스로의 승리를 확인하는 과정으로 변화하여 내적인 몰입도가 극대화됨.

## 4. 상세 설계 (Detailed Design)

### 4.1. 데이터 모델 (Data Model)

- **User (사용자)**: 닉네임과 수동 ID 기반의 인증 주체.
- **Workspace (워크스페이스)**: 팀원들이 모이는 전용 공간 (MVP는 1인 1워크스페이스).
- **Scoreboard (점수판)**: `목표 (WIG) > 후행지표 (Lag) > 선행지표 (Lead)`의 집합체.
- **Daily Log (일일 기록)**: 선행지표에 대한 요일별 O/X 성취 기록.

### 4.2. UI/UX 흐름 (UI/UX Flow)

- **Dashboard**: "이번 주 나의 승패"를 직관적으로 노출. WIG 게이지와 이번 주 선행지표 현황을 한 줄로 요약.
- **Meeting Mode**: 15분 회의를 위한 공약 체크, 결과 확인, 차주 공약 입력이 한 화면에서 이루어지는 흐름. 팀 단위로 사용 시 돌아가며 보고할 수 있는 뷰 제공.

### 4.3. 기간 탐색형 My Dashboard 확장안 (2026-03-16)

- 별도 문서로 분리했다.
- 기준 문서: `docs/planning/2026.03.16-dashboard-my-period-explorer-plan.md`

### 4.4. `dashboard/my` 기간·지표 선택형 Export 확장안 (2026-03-18)

- 별도 문서로 분리했다.
- 기준 문서: `docs/planning/2026.03.18-dashboard-my-export-plan.md`

### 4.5. Profile Preset Avatar 확장안 (2026-03-18)

- 별도 문서로 분리했다.
- 기준 문서: `docs/planning/2026.03.18-profile-preset-avatar-plan.md`

### 4.6. 인증 전략 정리: 폐쇄형 유지 + 공개 전환 준비 (2026-03-18)

- 별도 문서로 분리했다.
- 기준 문서:
  - `docs/planning/2026.03.18-auth-closed-beta-strategy.md`
  - `docs/planning/2026.03.18-public-transition-checklist.md`

### 4.7. 유료화 전략 검토 분리 (2026-03-18)

- 별도 문서로 분리해 관리한다.
- 기준 문서: `docs/planning/2026.03.18-monetization-strategy.md`

### 4.8. 팀 대시보드 메모 레일 확장안 (2026-03-25)

- 별도 문서로 분리했다.
- 기준 문서: `docs/planning/2026.03.25-team-dashboard-memo-rail-plan.md`

### 4.9. 온보딩 문구/플로우 다듬기 (2026-03-27)

- 별도 문서로 분리했다.
- 기준 문서: `docs/planning/2026.03.27-onboarding-copy-flow-plan.md`

## 5. 구체적 실행 방안 (Action Items)

### 기획 및 설계 (MVP 중심)

- [x] **[브랜딩]** 서비스명 확정: **WIG**
- [x] **[기획]** 기능 명세 기반 데이터베이스 테이블 구조(D1) 설계
- [x] **[디자인]** MVP 스펙 중심의 모바일 반응형 UI 설계 및 프로토타입 구축
- [ ] **[기획]** 위젯용 통계 집계 쿼리 및 로직 정의

### 구현 현황 업데이트 (2026-03-15)

- [x] **[백엔드]** Daily Log API 구현 완료
  - `GET /api/scoreboards/:id/logs/weekly`
  - `PUT /api/lead-measures/:id/logs/:date`
  - `DELETE /api/lead-measures/:id/logs/:date`
- [x] **[프론트엔드]** `dashboard/my` 화면을 실제 API 데이터로 전환
  - 활성 워크스페이스, 활성 점수판, 주간 로그를 Orval + TanStack Query로 조회
  - 주간 점수판 셀 클릭 시 `미기록 -> O -> X -> 미기록` 순환 토글 적용
  - 미래 날짜 비활성화, 로딩/빈 상태 분기 유지
- [x] **[프론트엔드]** Daily Log 낙관적 업데이트 및 실패 롤백 적용
- [x] **[기술부채]** Next App Router 동적 세그먼트 충돌 정리
  - `lead-measures/[leadMeasureId]` 계열을 `lead-measures/[id]`로 통일
  - `scoreboards/[scoreboardId]` 계열을 `scoreboards/[id]`로 통일
- [x] **[기술부채]** `getSession(db)` 타입 정의를 실제 Drizzle DB 타입 기준으로 보정

### 다음 액션

- [ ] **[프론트엔드]** `dashboard/my`의 confetti 축하 인터랙션 연결
- [ ] **[프론트엔드]** `measure/[id]`, `dashboard` 팀 뷰 등 남은 mock 의존 화면 API 연동
- [ ] **[품질]** 저장소 전역 TypeScript 오류와 `yarn lint` 스크립트 설정 문제 정리
- [ ] **[품질]** 백엔드 품질 보강 및 품질 게이트 강화 계획 실행
  - 기준 문서: `docs/planning/2026.03.17-backend-quality-hardening.md`
- [ ] **[문서]** 도메인별 구현 문서와 품질 기준 문서에 현재 검증 상태 반영
- [ ] **[기획]** 온보딩 문구/플로우 기준 확정
  - 용어 원칙: CTA는 `워크스페이스` 고정, 보조 설명에서만 `팀` 사용
  - 첫 진입 핵심 문구(랜딩/로그인/워크스페이스 없음 상태) 단일 카피 규칙 확정
- [ ] **[프론트엔드]** 온보딩 진입 카피/CTA 일괄 정리
  - 랜딩 CTA, 로그인/회원가입 보조 문구, 워크스페이스 생성/참가 화면 카피 적용
  - `워크스페이스 없음` 빈 상태 문구를 `생성 + 참가` 동시 선택 구조로 통일
- [ ] **[검증]** 온보딩 문구/플로우 회귀 점검
  - `가입/로그인 -> 워크스페이스 생성` 경로와 `가입/로그인 -> 초대코드 참가` 경로 수동 점검
  - 초대코드 입력 공백/소문자 케이스에서 입력 보정 동작 확인
- [x] **[프론트엔드]** 점수판 생성 화면 용어 코치마크 적용
  - `react-joyride` 3단계(가중목/후행지표/선행지표) 안내 적용
  - `localStorage` 1회 노출(`wig.setup.coachmark.v1.dismissed`) 적용
  - 코치마크 타이포그래피/버튼 색상을 서비스 톤(pretendard, primary)으로 정렬
- [x] **[기획]** 인증 운영 전략 확정
  - 현재 운영 모드를 `폐쇄형`으로 명시하고, 공개 회원가입은 별도 마일스톤으로 분리
  - 전역 어드민이 아니라 워크스페이스 운영자 기능만 MVP 범위로 정의
- [x] **[백엔드]** 폐쇄형 auth 최소 완성도 1차 보강
  - `POST /api/admin/users` 관리자 권한 체크 추가
  - 세션/권한/소유권 검증 누락 API 1차 점검
  - 마지막 관리자 보호 규칙과 자기 퇴출 금지 규칙 구현
- [x] **[프론트엔드]** 멤버 관리 최소 UI 구현
  - `설정 > 멤버 관리` 진입점 추가
  - 멤버 목록, 멤버 추가, 멤버 퇴출의 최소 흐름 구현
- [x] **[프론트엔드]** 워크스페이스 설정 최소 보강
  - 설정 화면에서 워크스페이스 이름 수정 가능
  - 별도 삭제 기능은 보류하고 운영 안정화 우선
- [x] **[기획]** 공개 전환 체크리스트 문서화
  - self-signup, 비밀번호 재설정, rate limit, abuse 대응, workspace 생성 정책, migration 규칙 정의
- [ ] **[기획]** `dashboard/my` 기간 선택 UX 확정
  - 기준 날짜 picker 위치, 주간/월간 segmented control, 빈 상태/로딩 상태 문구 정의
  - 주간 모드 날짜 선택이 "주 선택"이라는 점이 UI 라벨과 보조 문구에서 분명히 드러나도록 정의
  - 주간 모드와 월간 모드에서 각각 어떤 카드와 테이블을 보여줄지 확정
- [ ] **[프론트엔드]** `dashboard/my`에 `date` + `view` 기반 기간 탐색 상태 추가
  - URL query 동기화
  - 이전/다음 기간 이동 버튼과 오늘로 복귀 버튼 추가
- [ ] **[백엔드/API]** 선택 기간 조회 계약 정리
  - 주간 조회는 `weekStart` query를 실제로 주입해 임의 주 탐색을 지원
  - 월간 조회는 `month=YYYY-MM` 또는 동등한 기준 파라미터를 받아 임의 월 탐색을 지원
  - Orval 생성 코드와 대시보드 훅이 선택 기간에 따라 캐시 키를 분리하도록 보정
- [ ] **[프론트엔드]** 월간 모드 집계 UI 추가
  - 선행지표별 `달성 수 / 목표 / 달성률` 중심의 리스트 또는 테이블 추가
  - 월간 전용 지표와 주간 전용 지표를 구분 표시
- [ ] **[검증]** 기간 경계 케이스 점검
  - 월말/월초, 연도 경계, KST 자정 근처, 미래 날짜 선택, 점수판 없음 상태
- [ ] **[기획]** `dashboard/my` export 범위 확정
  - 지표 선택 규칙(전체/부분), 기간 최대 길이(최대 92일), 파일 기본 포맷(csv 우선) 확정
  - `from`/`to` 기본값을 현재 대시보드 보기 상태와 동기화하는 규칙 확정
- [ ] **[백엔드/API]** export 데이터 조회 계약 추가
  - 파일 바이너리가 아닌 JSON 기반 export 전용 조회 endpoint 추가
  - 응답 필드: `periodMeta`, `summary`, `leadMeasureBreakdown`, `dailyRows`
  - 기간 상한, 정렬 기준, 지표 필터 스키마를 OpenAPI에 명시
- [ ] **[프론트엔드]** Export 패널 및 다운로드 동작 구현
  - `dashboard/my`에 지표/기간 선택 UI 추가
  - 조건 검증 후 export API 호출 + csv 생성/다운로드
  - 로딩/실패/빈 데이터 상태 처리 및 토스트 피드백 추가
- [ ] **[검증]** Free 플랜 제약 대응 검증
  - 긴 기간 요청에서 응답 크기와 브라우저 생성 시간을 점검
  - 동일 조건 반복 다운로드 시 UI 멈춤/메모리 급증 여부 확인
- [ ] **[기획]** Profile preset avatar 시스템 상세 확정
  - preset 개수(초기 8~16개), `avatarKey` naming 규칙, 기본값 정책 정의
  - 프로필/팀 대시보드/향후 공용 user chip에서 동일 자산을 재사용하는 규칙 정리
- [ ] **[디자인]** 브랜드 가이드 기반 avatar icon 세트 제작
  - Linear Light Aesthetic에 맞는 line/stroke/background 규칙 정의
  - SVG 템플릿 기반으로 아이콘 초안 제작 후 8~16종 세트 확정
- [ ] **[백엔드/API]** Profile 계약 확장
  - `users.avatarKey` 저장 전략, migration, API 응답 필드 추가
  - `PUT /api/users/me`에서 허용 가능한 preset key validation 추가
- [ ] **[프론트엔드]** 프로필 아바타 선택 UX 구현
  - 프로필 화면 썸네일 선택 UI
  - `avatarKey` 기반 렌더링 유틸 및 fallback 규칙 추가
- [ ] **[검증]** preset avatar 회귀 점검
  - light 배경 대비, 작은 크기 가독성, 모바일 터치 선택성 확인
- [x] **[기획]** 팀 대시보드 메모 레일 UX 정의
  - 사용자별 `메모하기` / `메모보기` 분리
  - 오른쪽 보조 레일 배치와 본문 이동 원칙 정리
  - 메모 카드의 완료/삭제 액션 범위 정의
- [ ] **[프론트엔드]** 팀 대시보드 메모 레일 프로토타입 안정화
  - 사용자별 선택 상태와 레일 전환 규칙 정리
  - 레이아웃 충돌, 겹침, 가로 스크롤 없는지 점검
  - 모바일 스택 전환 동작 정의 및 보정
- [ ] **[기획]** 팀 메모 저장 정책 확정
  - 메모 작성자/대상 사용자/워크스페이스 공개 범위 정의
  - 수정/삭제 권한, 완료 처리 권한, 보존 기간 정책 정리
- [ ] **[백엔드/API]** 팀 메모 계약 필요 여부 판단
  - 프로토타입 검증 후 별도 메모 API를 만들지, dashboard 확장으로 흡수할지 결정
  - 저장이 필요하다면 조회/생성/완료/삭제 계약을 OpenAPI 기준으로 설계

## 6. 예상 비용 및 기간 (Cost & Estimation)

- **기간**: 기획 완료 후 MVP 개발까지 약 2~3주 예상
- **비용**: 1인 개발/운영 기준 Cloudflare 프리티어 범위 내에서 소화 가능.

## 7. 성공 및 실패 판단 기준 (Success & Failure Criteria)

- **성공**: 지인들과의 15분 가중목 회의 시, 스프레드시트 대신 이 서비스만 사용하여 보고와 계획이 완결될 때.
- **실패**: 점수판을 보고도 현재 내가 잘하고 있는지(Winning) 즉각적으로 파악되지 않을 때.

## 8. 구현 메모 (2026-03-15)

- `dashboard/my`는 현재 프로토타입 UI를 유지한 채 실제 세션 쿠키 기반 API를 사용한다.
- 사용자 닉네임 표시는 세션 기반 API 조회 결과를 기준으로 표시한다.
- 변경 파일 기준 ESLint는 통과했다.
- 저장소 전체 `yarn tsc --noEmit`는 이번 작업과 별개인 기존 타입 오류로 아직 통과하지 않는다.

## 9. 기간 선택형 대시보드 성공 기준 (2026-03-16)

- 사용자가 `dashboard/my`에서 임의 날짜를 골라 해당 주 또는 해당 월의 지표를 즉시 확인할 수 있다.
- 주간 모드에서는 어떤 날짜를 고르더라도 결과가 항상 1일치가 아닌 주간 7일 테이블로 일관되게 표시된다.
- 상단 요약 카드와 본문 테이블/리스트가 항상 같은 기간 기준으로 동기화된다.
- URL 공유 또는 새로고침 이후에도 선택한 기간과 보기 단위가 유지된다.
- 월간 모드에서 "이번 달 달성률"이 아니라 "선택한 월 달성률"로 의미가 명확하게 전달된다.

## 10. Preset Avatar 성공 기준 (2026-03-18)

- 사용자는 업로드 없이도 프로필에서 준비된 avatar 중 하나를 1회 선택하고 이후 일관되게 자신의 식별 이미지로 사용한다.
- `avatarKey`만으로 프로필 화면과 팀 대시보드에서 동일한 시각 결과가 재현된다.
- avatar asset은 브랜드 문서의 light/minimal 원칙을 따르며, 작은 크기에서도 서로 구분 가능하다.
- object storage 없이도 자산 관리와 배포가 가능하고, 운영자가 preset 추가/교체를 제어할 수 있다.

## 11. 인증 전략 성공 기준 (2026-03-18)

- 현재 운영 단계에서는 지인 중심 폐쇄형 사용이 불편 없이 유지된다.
- 관리자용 계정 생성 API와 워크스페이스 운영 기능이 최소한의 권한 규칙 아래 안전하게 동작한다.
- 추후 공개 전환 시 세션/프로필/워크스페이스 흐름을 버리지 않고 가입 정책만 확장할 수 있다.
- 공개 회원가입 관련 요구사항이 즉흥적으로 섞이지 않고 별도 마일스톤과 체크리스트로 관리된다.

## 12. 다음 단계 기획을 위해 필요한 정보 (Information Needed)

더 구체적인 설계를 위해 아래 정보들이 필요합니다.

### 9.1. 서비스 아이덴티티 (Identity)

- **가상의 서비스 이름**: (예: WIG-Master, WinLog, Focus-Board 등)
- **추구하는 톤앤매너**: (예: 진지하고 강력한 동기부여 vs 깔끔하고 모던한 대시보드 vs 가벼운 게임 같은 느낌)

### 9.2. 데이터 및 지표 상세 (Data Specs)

- **현재 사용 중인 노션/스프레드시트 샘플**: 어떤 지표(숫자, 시간, 완료여부 등)를 주로 관리하시는지에 대한 구체적인 예시.
- **지표의 복잡도**: 한 명의 사용자가 보통 몇 개의 선행지표를 관리하는지?

### 9.3. 상호작용 및 알림 (Interaction & Notification)

- **기록 리마인드**: 하루 중 언제 기록을 상기시켜주는 것이 가장 효과적일지?
- **팀 공유 노출도**: 지인들에게 내 어떤 데이터까지 공개하고 싶으신지? (단순 승패만 vs 구체적인 선행지표 달성 수치까지)

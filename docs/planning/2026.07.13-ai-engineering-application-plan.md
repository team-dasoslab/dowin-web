---
작성일시: 2026-07-13 00:00:00
최종갱신일시: 2026-08-13 00:00:00
목적: "Prompt Engineering, Context Engineering, Agent Engineering, MCP, Tool Calling, Harness, RAG, Eval, Review Automation 등 AI 활용 역량을 Dowin 프로젝트에 적용하기 위한 개념 정리와 실행 작업을 정의한다. (2026-08-13 갱신: 외부 리서치 기반 채점표와 3주간의 실제 진행 상황 반영을 8장으로, 그 채점표가 스스로 고른 리서치 프레임 밖의 갭 분석을 9장으로 추가)"
문서유형: planning
상태: draft
---

# AI Engineering 적용 계획

## 1. 배경

Dowin 저장소는 이미 `AGENTS.md`, `codex.md`, `.agents/skills/*`를 통해 AI Agent가 읽어야 할 문서, 작업 분류, 검증 루틴, 스킬별 책임을 제공한다.

현재 부족한 것은 새로운 AI 모델을 도입하는 것이 아니라, AI가 만든 결과를 평가하고, 실패를 축적하고, 다음 작업 품질을 개선하는 운영 루프다.

이 문서는 AI Engineering 관련 개념을 Dowin의 개발 운영에 맞게 해석하고, 실제로 적용할 작업을 정리한다.

## 2. 핵심 개념 정리

### 2.1. Prompt Engineering

Prompt Engineering은 좋은 지시문을 작성해 AI의 응답 품질을 높이는 방법이다.

핵심은 역할 부여, few-shot 예시, 출력 형식 지정, 제약 조건 명시다.

Dowin 적용 관점:

- 각 스킬의 `SKILL.md`가 저장소 전용 prompt 역할을 한다.
- 현재는 작업 유형별 지시가 있지만, 실패 사례를 반영해 prompt를 체계적으로 개선하는 루프는 약하다.

### 2.2. Context Engineering

Context Engineering은 프롬프트 하나가 아니라, AI가 판단할 때 참고할 정보 환경 전체를 설계하는 일이다.

핵심은 어떤 문서, 코드, 이전 대화, 도구 실행 결과를 언제 얼마나 넣을지 결정하는 것이다.

Dowin 적용 관점:

- `README.md`, `docs/onboarding.md`, `AGENTS.md`, `codex.md`, `.agents/skills/*`가 기본 context layer다.
- 하지만 작업별 context packet을 자동으로 만드는 도구는 아직 없다.

### 2.3. Agent Engineering

Agent Engineering은 AI가 한 번의 응답으로 끝나지 않고, 계획, 도구 호출, 결과 확인, 재판단을 반복하도록 실행 환경을 설계하는 일이다.

Dowin 적용 관점:

- `docs/planning/2026.04.09-dowin-agent-orchestration.md`가 agent workflow의 출발점이다.
- 현재는 단일 세션 중심 운영이고, 자동 handoff나 리뷰 자동화는 아직 약하다.

### 2.4. Tool Calling

Tool Calling은 LLM이 직접 외부 작업을 수행하는 것이 아니라, 호출할 함수와 인자를 구조화해 제안하고, 실제 실행은 외부 시스템이 담당하는 방식이다.

Dowin 적용 관점:

- Codex가 `rg`, `yarn test`, `apply_patch` 같은 도구를 사용하는 것이 개발 harness의 tool calling 사례다.
- 제품 내부 AI 기능을 만들 경우에도 LLM이 직접 DB를 수정하지 않고, 검증된 service/API를 호출하도록 경계를 둬야 한다.

### 2.5. MCP

MCP(Model Context Protocol)는 AI가 파일시스템, DB, 외부 API 같은 도구와 데이터 소스에 통일된 방식으로 연결되도록 만든 프로토콜이다.

Dowin 적용 관점:

- 현재 저장소에는 Dowin 전용 MCP 운영 표준이 없다.
- 외부 도구를 붙이기 전에 허용 도구, 권한 경계, 승인 기준, 결과 검증 기준을 먼저 정의해야 한다.

### 2.6. Harness

Harness는 모델이 도구를 호출하고, 결과를 받고, 다시 판단하는 실행 루프와 환경 전체를 뜻한다.

Dowin 적용 관점:

- Codex, Cursor, Claude Code 같은 도구는 각각 다른 harness다.
- Dowin은 `AGENTS.md`, `codex.md`, `.agents/skills/*`로 Codex harness에 맞는 작업 환경을 만들고 있다.
- 다만 harness 보안과 품질 개선 루프는 더 강화해야 한다.

### 2.7. RAG

RAG(Retrieval-Augmented Generation)는 답변 생성 전에 관련 문서를 검색해 context에 주입하는 방식이다.

Dowin 적용 관점:

- 현재는 사람이 문서 읽기 순서를 지정하는 방식에 가깝다.
- 향후에는 변경 파일이나 작업 유형을 기준으로 관련 문서를 자동 검색해 Agent에게 제공할 수 있다.

### 2.8. Context Management

Context Management는 context window가 유한하다는 제약 안에서 무엇을 넣고 뺄지, 언제 요약할지, 어떤 정보를 장기 기억으로 남길지 결정하는 전략이다.

Dowin 적용 관점:

- 작업이 길어질수록 읽은 파일, 명령 결과, 사용자 요구가 섞여 context가 커진다.
- 단계별 요약, handoff contract, failure category를 더 엄격히 쓰면 context 오염을 줄일 수 있다.

### 2.9. Memory

Memory 관리는 세션이 끝나도 남길 정보를 저장하고, 다음 작업에서 필요한 시점에 불러오는 구조다.

Dowin 적용 관점:

- 현재 long-term memory 역할은 문서와 스킬이 담당한다.
- 실패 사례, 자주 쓰는 context packet, 리뷰 결과는 별도 memory artifact로 남길 필요가 있다.

### 2.10. Evaluation

Evaluation은 특정 태스크에서 AI 출력이 기준에 맞는지 측정하는 체계다.

Dowin 적용 관점:

- 현재는 코드 테스트와 린트는 있지만 AI 산출물 자체를 평가하는 eval은 없다.
- Golden case, rubric, failure category를 먼저 만들어야 한다.

### 2.11. Benchmark

Benchmark는 표준화된 데이터셋과 메트릭으로 모델, 프롬프트, 설정을 비교하는 방식이다.

Dowin 적용 관점:

- 외부 benchmark보다 Dowin 작업에 맞는 내부 golden dataset이 더 중요하다.
- 예: API 계약 변경, 프론트 API 연동, WebView bridge 변경, 결제 권한 변경.

### 2.12. Prompt 버전 관리

Prompt 버전 관리는 프롬프트와 스킬을 코드처럼 관리하고, 변경 이유와 결과를 추적하는 방식이다.

Dowin 적용 관점:

- `.agents/skills/*`는 git으로 관리되지만, 변경 이유와 성능 변화 기록은 약하다.
- 스킬 변경 changelog와 eval 결과 연결이 필요하다.

### 2.13. Review Automation

Review Automation은 PR 변경 범위를 분석해 자동으로 위험을 태깅하거나 리뷰 코멘트를 남기는 방식이다.

Dowin 적용 관점:

- 현재 CI는 lint, type check, test, build, preview smoke 중심이다.
- 변경 파일 기반으로 quality/security/performance/harness 리뷰가 자동으로 떠오르는 구조가 필요하다.

### 2.14. LLM 동작 원리

#### Tokenization

Tokenization은 텍스트를 모델이 처리할 수 있는 토큰 단위로 나누는 과정이다.

Dowin 적용 관점:

- 한국어 문서와 긴 코드 context는 토큰 비용을 빠르게 키운다.
- 작업별 context를 선별하지 않으면 비용과 지연 시간이 늘고, 중요한 정보가 묻힐 수 있다.

#### Context Window

Context Window는 모델이 한 번에 참고할 수 있는 최대 토큰 범위다.

Dowin 적용 관점:

- 모든 문서를 다 넣는 방식보다 작업별 필요한 문서만 넣는 방식이 안정적이다.

#### KV Cache / Prompt Caching

KV Cache는 이전 토큰의 attention 계산 결과를 재사용해 다음 토큰 생성을 빠르게 하는 방식이다.

Dowin 적용 관점:

- 자주 반복되는 시스템 지시, 스킬, 저장소 규칙은 안정적인 앞쪽 context로 유지하고, 매번 바뀌는 사용자 요청과 변경 파일 정보는 뒤쪽에 배치하는 전략이 유리하다.

#### Decoding / Reasoning

Decoding은 모델이 다음 토큰을 선택하는 방식이며, reasoning은 복잡한 문제를 단계적으로 풀도록 하는 모델 또는 harness의 동작 방식이다.

Dowin 적용 관점:

- 코드 수정, 보안 검토, DB 권한 변경은 낮은 무작위성과 충분한 검증 루프가 필요하다.
- 카피, 랜딩, 문구 실험은 더 넓은 탐색이 유효할 수 있다.

## 3. Dowin 현재 상태 진단

### 3.1. 강점

- AI Agent용 진입 문서가 있다.
- 작업 유형별 로컬 스킬이 있다.
- 백엔드, 프론트엔드, WebView, 품질, 성능, 보안, 하네스 보안이 분리되어 있다.
- OpenAPI, Orval, Zod, 테스트, CI가 있어 rule-based 검증 기반이 있다.
- 하네스 변경 시 별도 보안 점검을 요구하는 규칙이 있다.

### 3.2. 부족한 점

- AI 산출물 전용 eval이 없다.
- Golden case와 benchmark dataset이 없다.
- 스킬/prompt 변경 이력과 결과 품질이 연결되어 있지 않다.
- PR 변경 범위 기반 리뷰 자동화가 없다.
- Context packet 자동 생성이 없다.
- MCP/tool 운영 표준이 없다.
- 제품 내부 AI 기능의 우선순위가 현재 Basic paid value와 다시 정렬되어 있지 않다.

### 3.3. 적용 원칙

- 우선순위는 제품 내부 AI 기능보다 개발 운영 보조에 둔다.
- 가능한 작업은 로컬 모델, 로컬 스크립트, 규칙 기반 분석으로 먼저 처리한다.
- PR 코드 리뷰처럼 외부 LLM/API 호출이 필요한 기능은 당장 도입하지 않는다.
- 자동화는 처음부터 PR comment bot으로 가지 않고, CI summary 또는 로컬 리포트 출력부터 시작한다.
- AI 판단이 들어가는 경우에도 최종 merge, 배포, DB 변경, 보안 판단 책임은 사람에게 둔다.

## 4. 적용 작업 목록

### 4.1. P0: AI 작업 품질 Eval 만들기

목표:

- AI 작업 결과를 사람이 감으로만 판단하지 않고 반복 가능한 기준으로 평가한다.

작업:

- `docs/dev/common/` 또는 `docs/planning/`에 AI 작업 eval 기준 문서를 만든다.
- 대표 golden case 5개를 정의한다.
  - API 계약 변경
  - 프론트 API 연동
  - WebView bridge 변경
  - 결제/권한 변경
  - 문서/온보딩 정리
- 각 case마다 성공 기준을 적는다.
  - 요구사항 충족
  - 변경 범위 준수
  - 테스트 추가 여부
  - 문서 갱신 여부
  - 보안/권한 누락 여부
- 실패 카테고리를 기존 오케스트레이션 문서의 `failure_categories`와 연결한다.

산출물:

- `docs/planning/YYYY.MM.DD-ai-work-evaluation-plan.md`
- Golden case 목록
- Eval rubric

### 4.2. P0: `quality-check` 스킬에 AI 산출물 확인 항목 추가

목표:

- 품질 점검이 코드 테스트뿐 아니라 AI 작업 특유의 실패도 잡도록 한다.

작업:

- `.agents/skills/quality-check/SKILL.md`에 AI 산출물 검토 항목을 추가한다.
- `references/quality-rules.md`에 다음 항목을 넣는다.
  - 요구사항 누락
  - 과잉 수정
  - 관련 없는 리팩터링
  - 문서-구현 불일치
  - 테스트 없이 동작 변경
  - OpenAPI와 구현 불일치
- 리뷰 결과 보고 형식에 `ai_output_risk` 카테고리를 추가할지 검토한다.

산출물:

- 갱신된 `dowin-quality-check`
- AI 작업 검토 체크리스트

### 4.3. P1: 스킬/프롬프트 변경 이력 관리

목표:

- 스킬 변경이 왜 일어났고 어떤 실패를 줄이기 위한 것인지 추적한다.

작업:

- `.agents/skills/CHANGELOG.md` 또는 `docs/planning/ai-skill-change-log.md` 위치를 정한다.
- 스킬 변경 시 아래 항목을 기록한다.
  - 변경 날짜
  - 대상 스킬
  - 변경 이유
  - 기대 효과
  - 연결된 실패 카테고리
  - 후속 eval 필요 여부
- 하네스 관련 변경은 `dowin-harness-security-check` 대상임을 명시한다.

산출물:

- 스킬 변경 로그
- prompt 버전 관리 규칙

### 4.4. P1: AI 작업 실패 사례 로그 만들기

목표:

- 반복 실패를 개인 기억이 아니라 프로젝트 자산으로 남긴다.

작업:

- 실패 사례를 저장할 문서 위치를 정한다.
- 최소 기록 형식을 정의한다.
  - 작업 요약
  - 실패 증상
  - 원인
  - 실패 카테고리
  - 재발 방지 스킬/문서 수정
  - 관련 테스트
- 실패 사례가 3회 이상 반복되면 스킬이나 onboarding 문서를 수정하는 규칙을 둔다.

산출물:

- AI 작업 실패 사례 로그
- 반복 실패 대응 규칙

### 4.5. P2: 로컬/규칙 기반 PR 위험 태깅 자동화

목표:

- 외부 AI 코드 리뷰 API 없이, 변경 파일에 따라 필요한 리뷰가 자동으로 드러나게 한다.

범위:

- 포함:
  - 변경 파일 패턴 분석
  - 필요한 스킬/검증 명령 추천
  - CI summary 또는 로컬 리포트 출력
  - 보안/성능/하네스 변경 감지
- 제외:
  - 외부 LLM API 기반 코드 리뷰
  - PR inline comment bot
  - 자동 승인/자동 merge
  - 코드 의미를 LLM이 판정하는 고비용 리뷰

작업:

- 파일 패턴별 리뷰 게이트를 정의한다.
  - `src/app/api/**`, `src/domain/**`: backend, quality, security
  - `src/api-spec/openapi.yaml`: backend, frontend, quality
  - `src/lib/server/**`: backend, security
  - `src/app/**`, `src/components/**`, `src/hooks/**`: frontend, quality
  - `src/lib/bridge.ts`, `src/types/bridge.ts`, app webview docs: frontend-webview, security
  - `.agents/**`, `AGENTS.md`, `codex.md`: harness-security
  - storage/query/aggregation 변경: performance
- GitHub Actions에서 변경 파일 목록을 읽어 체크리스트를 출력하는 workflow를 검토한다.
- 처음에는 comment bot보다 CI summary 출력부터 시작한다.
- 로컬에서 같은 결과를 볼 수 있도록 `yarn` script 또는 작은 Node 스크립트를 검토한다.

산출물:

- 변경 파일 패턴별 review gate mapping
- 로컬 review checklist 스크립트 초안
- PR review checklist workflow 초안

### 4.6. P2: Context Packet 표준화

목표:

- Agent가 매번 탐색 비용을 반복하지 않도록 작업별 필수 context를 묶는다. 무거운 KI(Knowledge Item) 시스템 도입 전, 가벼운 "Working Set" 문서로 시작한다.

작업:

- 작업 유형별 context packet을 정의한다.
  - **Backend API Change Packet**
    - 먼저 볼 파일: `src/api-spec/openapi.yaml`, `src/db/schema.ts`, 해당 도메인 로직
    - 금지 사항: OpenAPI 갱신 없이 라우터 먼저 수정 금지, `drizzle-kit push` 로컬 수동 적용 금지
    - 검증 명령: `yarn gen:api`, `yarn test:backend`
  - **Frontend API Integration Packet**
    - 먼저 볼 파일: `src/api/generated/` 훅, 해당 도메인의 `_hooks/`, UI 컴포넌트
    - 금지 사항: 페이지 컴포넌트 내부에 복잡한 상태/폼 로직 혼재 금지
    - 검증 명령: `yarn test:frontend`, `yarn tsc --noEmit`
  - **Billing Entitlement Packet**
    - 먼저 볼 파일: 결제 도메인 로직, 스키마, 권한 체크 서비스
    - 금지 사항: 권한 우회 가능성 있는 느슨한 체크 금지, 보안 승인 전 배포 금지
    - 검증 명령: `yarn test:billing` (또는 관련 테스트), `dowin-security-check`
  - **WebView Bridge Packet**
    - 먼저 볼 파일: `src/lib/bridge.ts`, `src/types/bridge.ts`, `docs/dev/app-webview/`
    - 금지 사항: 모바일 fallback 검증 없이 bridge 호출 금지
  - **Product Update Packet**
    - 먼저 볼 파일: `src/content/product-updates.ts`, `src/lib/product-updates.ts`, 업데이트 페이지 또는 대시보드 카드 영향 범위
  - **Harness/Security Packet**
    - 먼저 볼 파일: `AGENTS.md`, `.agents/skills/*`, `codex.md`
- 각 packet 문서를 `docs/planning/` 하위 또는 별도 `docs/dev/common/context-packets.md`에 명문화한다.
- 체인의 첫 실행 스킬(`dowin-intake`, 이후 `dowin-planning`/`dowin-backend-api-spec`/`dowin-frontend-ui` 등)이 작업 분류 후 가장 먼저 이 packet을 로드하여 탐색(rg) 범위를 좁히도록 JIT 탐색 규칙에 추가한다. (2026-08-01: 별도 `dowin-orchestrator` 스킬은 삭제됨 — `docs/planning/2026.04.09-dowin-agent-orchestration.md` 참고)

산출물:

- Context packet 상세 문서 (`docs/dev/common/context-packets.md` 등)
- Orchestrator 스킬 및 도메인 스킬 내 JIT 규칙 갱신 완료

### 4.7. P2: MCP / Tool 운영 기준 정리

목표:

- 외부 도구를 붙일 때 권한과 검증 기준을 먼저 정한다.

작업:

- 허용할 tool/MCP 범주를 정의한다.
  - 파일 읽기
  - 코드 검색
  - 테스트 실행
  - 문서 제어
  - 배포/인프라
  - DB/운영 데이터
- 각 범주의 승인 기준을 정한다.
- 외부 tool 결과를 신뢰하기 전 필요한 검증을 정의한다.
- destructive command, remote migration, production deploy, secret 접근은 별도 승인 대상으로 고정한다.

산출물:

- Tool/MCP 운영 기준 문서
- Harness security checklist 확장

### 4.8. P3: 제품 내부 AI 기능 재정렬

목표:

- AI 기능을 현재 Dowin의 Basic paid value와 직접 연결한다.

작업:

- 기존 AI 실행계획 리뷰 문서를 현재 제품 언어 기준으로 재검토한다.
- AI 기능 후보를 다시 분류한다.
  - 계획 점검
  - 체크인 응답 요약
  - 운영 리포트 보조
  - 리더 개입 추천
  - 액션 아이템 개선 제안
- MVP 후보를 하나로 좁힌다.
- 생성형 AI가 필요한 영역과 규칙 기반으로 충분한 영역을 나눈다.
- 비용, 지연 시간, 개인정보, 오답 위험, 사용자 수정 UX를 정의한다.

산출물:

- 제품 내부 AI 기능 재기획 문서
- MVP/Post-MVP 경계
- API/UX/검증 초안

## 5. 권장 실행 순서

1. AI 작업 eval 기준과 golden case를 먼저 만든다.
2. `quality-check` 스킬에 AI 산출물 검토 항목을 추가한다.
3. 스킬 변경 로그와 실패 사례 로그를 만든다.
4. 외부 API 없는 변경 파일 기반 PR 위험 태깅을 도입한다.
5. Context packet을 표준화한다.
6. MCP/tool 운영 기준을 정리한다.
7. 제품 내부 AI 기능은 마지막에 현재 유료 가치와 맞춰 다시 기획한다.

## 6. 운영 원칙

Dowin에서 AI를 활용한다는 것은 단순히 Codex나 Cursor 같은 도구를 쓰는 것이 아니다.

현재는 저장소 문서, 스킬, 검증 명령을 통해 AI가 일할 수 있는 context와 harness를 만들고 있다. 다음 단계는 AI 산출물을 eval로 측정하고, 실패 사례를 skill/prompt 개선에 되먹임하며, PR 단계에서 필요한 리뷰 체크리스트를 로컬/규칙 기반으로 자동화하는 것이다.

즉, 목표는 AI에게 모든 작업을 맡기는 것이 아니라, 사람이 목표와 품질 기준을 설계하고 AI가 반복 작업을 안정적으로 수행하도록 개발 환경을 만드는 것이다.

외부 LLM/API 기반 PR 코드 리뷰는 비용, 노이즈, 보안, 판단 책임 문제가 있으므로 현재 단계에서는 보류한다. 먼저 로컬에서 재현 가능한 규칙 기반 자동화와 문서화된 eval 기준을 갖춘 뒤 필요성을 다시 판단한다.

## 7. 완료 기준

- AI 작업 eval 기준 문서가 존재한다.
- Golden case가 최소 5개 정의되어 있다.
- `dowin-quality-check`가 AI 산출물 특화 실패를 점검한다.
- 스킬 변경 이력과 실패 사례 로그가 운영된다.
- PR 변경 범위 기반 리뷰 게이트가 문서화되어 있다.
- MCP/tool 운영 기준이 하네스 보안 기준과 연결되어 있다.
- 제품 내부 AI 기능 후보가 현재 Basic paid value 기준으로 재정렬되어 있다.

## 8. 외부 리서치 기반 AI-Native 채점표 (2026-08-13 갱신)

(Linear DAS-59 / beads `dowin-web-u5z`. 이번 갱신 범위는 리서치·채점표 작성·현재 상태 채점까지이며, 채점 결과에 따른 실제 개선 작업은 아래 8.5의 결과를 사용자와 논의해 우선순위를 정한 뒤 별도로 진행한다.)

3장(현재 상태 진단)을 작성한 지 3주가 지나는 동안 스킬 체계 재구성, CodeGraph 도입, beads 이슈 트래커 정착, Output Contract 통일, 커밋 컨벤션 스킬, 웹훅 가드 등 실제 진행이 있었다. 이 장은 그 진행 상황을 업계 리서치 근거와 함께 다시 채점해, 무엇이 실제로 채워졌고 무엇이 여전히 비어 있는지 구분한다.

### 8.1 리서치 출처

- Anthropic, [Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) — "가장 적은 고신호 토큰" 원칙, compaction/구조화된 노트, 메모리 도구
- Anthropic, [Effective harnesses for long-running agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) — 생성/평가 분리, fresh-context evaluator, `claude-progress.txt` 패턴
- dos Santos, Costa, Montandon, Silva, Valente, [Configuration Smells in AGENTS.md Files](https://arxiv.org/abs/2606.15828) (arXiv:2606.15828) — AGENTS.md/CLAUDE.md 100개 저장소 분석, 6대 설정 스멜과 탐지 기준
- AGENTS.md 표준 명세 (2025-08 OpenAI/Google/Cursor/Factory 공동 제정, 2025-12 Linux Foundation Agentic AI Foundation 기증) — [agents.md](https://agents.md/)
- Google Cloud DORA, [State of AI-assisted Software Development 2025](https://dora.dev/dora-report-2025/) 및 [AI Capabilities Model](https://dora.dev/ai/capabilities-model/report/) — AI는 증폭기(amplifier)라는 핵심 결론과 7대 역량
- HumanLayer, [12-Factor Agents](https://github.com/humanlayer/12-factor-agents) 계열 자료 — context 소유, prompt 버전관리, 결정론적 코드와 LLM 판단 지점의 전략적 분리
- Claude Platform Docs, [Agent Skills / Best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) — progressive disclosure(3단계 로딩), SKILL.md 500줄 권장선
- SWE-bench 계열, TDAD(Test-Driven Agentic Development, arXiv:2603.17973) — Fail-to-Pass/Pass-to-Pass 회귀 검증 패턴

### 8.2 채점 기준

- `O` = 충족 (레포에서 실제로 확인됨) · `△` = 부분 충족/트레이드오프 있음 · `X` = 미충족
- 모든 항목은 실제 파일·커밋·명령 실행 결과로 확인한 것만 표기했다 (추정 표기 없음).

### 8.3 채점표

**A. Context/Instruction 파일 — 근거: AGENTS.md 표준, Configuration Smells 논문**

| 항목                                                             | 상태 | 근거                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A1. 루트에 AGENTS.md/CLAUDE.md 존재                              | O    | `AGENTS.md`(303줄), `CLAUDE.md`(79줄)                                                                                                                                                                                                |
| A2. Context Bloat 회피 (논문 탐지 기준: ≥200줄)                  | △    | `AGENTS.md`가 303줄로 논문의 200줄 기준을 넘음. 다만 전문 지시는 스킬로 분리되어 있어(A3) 실질적 밀도는 낮음                                                                                                                         |
| A3. Skill Leakage 회피 (전문 지시는 별도 스킬 파일로)            | O    | `.agents/skills/*`, `.claude/skills/*` 미러 구조로 도메인별 분리                                                                                                                                                                     |
| A4. Lint Leakage 회피 (린터가 이미 강제하는 규칙 중복 기술 금지) | △    | `AGENTS.md`의 "NO any TYPE ALLOWED" 규칙이 `eslint.config.mjs:30`의 `@typescript-eslint/no-explicit-any: error`와 중복. 다만 `@ts-ignore`/`eslint-disable` 우회 금지까지 포함해 린터 단독으로는 못 막는 부분도 있어 순수 중복은 아님 |
| A5. Blind Reference 회피 (외부 문서 참조 시 맥락 설명)           | O    | 각 스킬 링크에 "언제 쓰는지" 설명이 붙어 있음                                                                                                                                                                                        |
| A6. Init Fossilization 회피 (최초 생성 후 지속 갱신)             | O    | 최근 3주간 AGENTS.md 관련 커밋 다수(`91453327` 이전 `a69fe931` 오케스트레이션 v2, CodeGraph 규칙 강화 등)                                                                                                                            |
| A7. 안전 가드레일 명시 (secret 파일 금지, 프로덕션 명령 컨펌)    | O    | "Safety Guardrails (Hard Rules)" 절 + `.claude/settings.json` deny list 병행                                                                                                                                                         |

**B. 코드 탐색/리트리벌 — 근거: Anthropic context engineering (고신호 토큰 원칙)**

| 항목                                                    | 상태 | 근거                                                                    |
| ------------------------------------------------------- | ---- | ----------------------------------------------------------------------- |
| B1. 사전 계산된 코드 인덱스 존재                        | O    | `.codegraph/`(daemon 구동 중, 40MB DB)                                  |
| B2. grep/find보다 인덱스 우선 사용을 하드룰로 강제      | O    | AGENTS.md "Code Navigation" — "mandatory, not a preference"             |
| B3. 인덱스가 안 잡는 비-코드 자산에 대한 폴백 경로 명시 | O    | "Fall back to path-based search only for..." (openapi.yaml, 문서, 설정) |

**C. 작업 상태/영속 메모리 — 근거: DORA 강한 버전관리, Anthropic 메모리 도구, 12-factor "own your context"**

| 항목                                                                       | 상태 | 근거                                                                                                    |
| -------------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------- |
| C1. 세션 밖에 영속하는 작업 트래커                                         | O    | beads(Dolt DB, `.beads/`), 임베디드 모드 정상 동작 확인                                                 |
| C2. 작업이 단계별 하위 이슈로 분해되고 단계 종료 시 클로즈                 | O    | 예: `dowin-web-4h9` 에픽 아래 4개 child task 모두 완료 클로즈                                           |
| C3. 실패/교훈이 영속 메모리로 축적 (`bd remember`)                         | X    | `bd memories` 실행 결과 "No memories stored." — 규칙은 문서화돼 있으나(CLAUDE.md) 실제 사용 0건         |
| C4. 장기 실행 에이전트를 위한 진행상황 파일 패턴 (`claude-progress.txt`류) | X    | 세션 재개 시 상태 복원은 beads 이슈 상태에 의존. 세션 중간에 남기는 별도 progress 로그 파일 패턴은 없음 |

**D. 스킬/도구 설계 — 근거: Claude Agent Skills progressive disclosure**

| 항목                                               | 상태 | 근거                                                             |
| -------------------------------------------------- | ---- | ---------------------------------------------------------------- |
| D1. SKILL.md(핵심)+references/(세부) 분리 구조     | O    | 각 스킬 디렉터리에 `SKILL.md` + `references/*.md`                |
| D2. SKILL.md 본문이 권장 분량(약 500줄) 이내       | O    | 확인한 스킬(`backend-quality-check` 등)은 100줄 내외             |
| D3. 스킬 간 선택 모호성을 줄이는 트리거 예시 제공  | O    | AGENTS.md "Trigger examples" 절에 스킬별 실제 발화 예시          |
| D4. 스킬 개수 증가에도 체인 순서가 명확히 문서화됨 | O    | "Project Skills" 절에 전체 체인 순서를 번호 없이도 순서대로 나열 |

**E. 검증 루프/Evals — 근거: Anthropic effective harnesses(생성/평가 분리), TDAD F2P/P2P**

| 항목                                           | 상태 | 근거                                                                                                                                                                                        |
| ---------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1. AI 산출물 자체를 채점하는 rubric 존재      | O    | `docs/planning/2026.07.14-ai-work-evaluation-plan.md` (O/X/N/A 체크리스트 + golden case 5개)                                                                                                |
| E2. 생성과 평가가 별도 단계/스킬로 분리        | △    | `dowin-backend` → `dowin-backend-quality-check`로 스킬은 분리되지만, 같은 대화 컨텍스트 내에서 이어지는 경우가 많아 Anthropic이 권장하는 "빌드를 본 적 없는 fresh-context evaluator"는 아님 |
| E3. 실행 가능한 검증 명령이 필수 단계로 강제됨 | O    | "Verification Defaults" 절에 `yarn lint`/`tsc`/`test:frontend`/`test:backend` 고정                                                                                                          |
| E4. 기능 검증(F2P)과 회귀 검증(P2P) 구분 실천  | △    | Vitest 실행은 있으나 F2P/P2P 개념으로 테스트를 구분해 관리하지는 않음                                                                                                                       |
| E5. Golden case/벤치마크가 최신 상태로 유지    | △    | 5개 golden case는 정의돼 있으나(7/14) 이후 실측 갱신 여부는 문서상 확인 불가                                                                                                                |

**F. 가드레일/안전 — 근거: AGENTS.md 자체, Veracode OWASP 취약점 통계, DORA "명확한 AI 입장"**

| 항목                                                        | 상태 | 근거                                                                          |
| ----------------------------------------------------------- | ---- | ----------------------------------------------------------------------------- |
| F1. 비밀/프로덕션 영향 명령에 대한 하드룰 + 메커니즘적 강제 | O    | Safety Guardrails 절 + `.claude/settings.json` deny list                      |
| F2. 타입 안전성 강제 (any/ts-ignore/eslint-disable 금지)    | O    | "AI Code Generation Constraints" 절                                           |
| F3. 보안 특화 리뷰 스킬 존재                                | O    | `backend-security-check`, `frontend-security-check`, `harness-security-check` |
| F4. 커밋/푸시/머지에 대한 사람 승인 경계 명시               | O    | "Review Before Commit" — `dowin-release` 예외만 허용                          |

**G. 관측성/지속 개선 — 근거: 12-factor "own your prompts", DORA amplifier 인사이트**

| 항목                                                     | 상태 | 근거                                                                                                                                                |
| -------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1. 스킬/프롬프트 변경 이력 추적                         | O    | `.agents/skills/CHANGELOG.md` — 실제 항목 6건 기록(템플릿 제외)                                                                                     |
| G2. 실패 카테고리 표준 분류 체계                         | O    | CHANGELOG.md 내 17개 `failure_categories` 표, 전 스킬 공용                                                                                          |
| G3. PR/변경 위험도 파일 패턴 기반 자동 태깅              | X    | `.github/workflows/`에 관련 워크플로 없음. 4.5절 작업이 아직 미착수                                                                                 |
| G4. Context Packet(작업유형별 필수 컨텍스트 묶음) 표준화 | △    | 4.6절에서 설계는 했으나 `docs/dev/common/context-packets.md` 같은 산출물은 없음. 각 스킬의 "JIT Search Strategy" 절이 부분적으로 같은 역할을 대신함 |

**H. 조직/프로세스 정렬 — 근거: DORA AI Capabilities Model (7대 역량)**

| 항목                                                                        | 상태 | 근거                                                                              |
| --------------------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------- |
| H1. 작은 배치로 작업/커밋 분해                                              | O    | "스테이지당 최소 4회 분리 커밋" 규칙                                              |
| H2. 버전관리 실천 (Conventional Commits, 릴리스 자동화)                     | O    | `docs/dev/common/versioning.md`, `release-please.yml`                             |
| H3. AI 도입에 대한 명확하고 문서화된 입장                                   | O    | AGENTS.md의 Collaboration Style/Safety Guardrails가 곧 "AI stance" 문서 역할      |
| H4. AI가 접근 가능한 고품질 내부 데이터/플랫폼 (DORA가 꼽은 최대 증폭 요인) | O    | CodeGraph + beads + skills 조합 자체가 이 역량에 해당 — 8장 전체에서 가장 강한 축 |

### 8.4 총점 요약

- O 26 / △ 6 / X 3 (총 35개 항목)
- 가장 강한 축: **B(코드 탐색)**, **F(가드레일)**, **H(조직 정렬)** — 전 항목 O
- 가장 약한 축: **C(작업 상태/영속 메모리)** — 4개 중 2개 X (`bd remember` 미사용, progress 파일 패턴 부재)
- 구조는 이미 갖춰져 있으나 "실천 기록"이 비어 있는 항목이 두드러짐 (C3 메모리 0건, G3 자동화 미착수)

### 8.5 갭 우선순위 (개선 후보 — 실행은 별도 논의 후)

기존 4장 작업 목록과 겹치는 항목은 번호를 병기했다. 매겨진 우선순위는 제안이며, 실제 착수 여부와 순서는 사용자와 논의해 정한다.

1. **[C3] `bd remember` 실사용 시작** — 규칙은 이미 있으나 실제로 0건. 반복되는 실패/결정을 기록하는 습관화가 구조 추가보다 우선순위가 높음 (비용 거의 없음, 즉시 시작 가능)
2. **[G3 / 4.5] 파일 패턴 기반 PR 위험 태깅** — 여전히 미착수. 외부 LLM 리뷰 API 없이 CI summary로 시작하는 안이 4.5절에 이미 설계돼 있음
3. **[신규] 생성/평가 컨텍스트 분리(E2) 강화** — Anthropic 권장인 "빌드를 본 적 없는 평가자"에 가깝게, quality-check 단계 진입 시 새 세션/서브에이전트로 넘기는 방식 검토
4. **[신규] 장기 실행 진행상황 파일(C4)** — 세션이 끊겼다가 재개될 때 beads 이슈 상태 외에, 무엇을 시도했고 무엇이 막혔는지 남기는 `*-progress.md` 류 패턴 검토
5. **[G4 / 4.6] Context Packet 표준화 마무리** — 설계는 끝났으나 산출물 문서화가 안 됨. 착수 비용 대비 재탐색 비용 절감 효과가 있어 우선순위 있음
6. **[4.7] MCP/Tool 운영 기준 문서** — 아직 별도 산출물 없음. 현재 MCP 연동(Linear, CodeGraph)이 이미 실사용 중이라 사후적으로라도 정리할 가치 있음
7. **[A2/A4] AGENTS.md 분량·중복 정리** — 200줄 기준 초과와 any-금지 규칙 중복은 논문 기준으로는 "스멜"이지만, 이 프로젝트는 의도적으로 강조를 위해 중복시켰을 가능성이 있어 우선순위는 낮음. 실제로 에이전트가 그 규칙을 놓치는 사례가 관찰되면 그때 정리

이 중 실제 착수할 항목·순서는 사용자 확인 후 별도 beads 이슈로 분리해 진행한다 (이번 DAS-59/`dowin-web-u5z` 범위는 문서화·채점까지).

## 9. 리서치 프레임 밖 관점 — AX 확장 갭 분석 (2026-08-13)

### 9.1 배경

8장의 채점표는 8.1에 나열한 리서치 소스를 기준으로 스스로를 채점한 결과다. 이 구조는 "그 소스들이 다루는 항목"에서는 신뢰할 수 있지만, **그 소스들이 애초에 다루지 않는 항목**은 채점표에 나타나지 않는다 — 자기 참조적 채점의 구조적 한계다. 이 장은 특정 리서치 소스 목록에 묶이지 않고, 조직이 AI를 개발/제품 프로세스에 도입할 때 일반적으로 점검하는 넓은 관점에서 이 저장소가 "할 수 있었는데 안 한 것"을 별도로 정리한다. 8장과 마찬가지로 실제 파일·커밋·스킬 내용을 확인한 근거만 남긴다.

### 9.2 갭 목록

**1. 결과(outcome) 측정이 산출물 품질 체크리스트로만 대체되어 있다**

`docs/planning/2026.07.14-ai-work-evaluation-plan.md`는 전량 산출물 품질 체크리스트다(범위 준수, 테스트 존재, 보안, 문서 동기화 등, O/X/N/A). AI 도입 자체가 효과가 있는지 — PR 처리 시간 변화, 리뷰 rework 비율, 세션당 재시도 횟수, 토큰/비용 대비 산출 — 를 재는 지표는 없다. 8장이 DORA 리포트를 근거로 인용했지만(H4), DORA 모델의 핵심은 "역량이 실제 결과로 이어지는지 측정"하는 것이고, 이 저장소는 역량 존재 여부(8장 채점표 자체)만 재고 결과는 재지 않는다.

**2. 런타임에 신뢰할 수 없는 외부 콘텐츠에 대한 방어 원칙이 없다**

`.agents/skills/harness-security-check/SKILL.md`를 확인한 결과, 체크리스트에 "prompt injection or unsafe instruction patterns" 항목이 있지만 스코프는 AGENTS.md/codex.md/skill 파일 같은 **정적 지시 파일 리뷰**로 한정된다. 이 저장소는 이미 Linear MCP, Google Drive MCP, CodeGraph MCP를 실사용 중인데, 런타임에 MCP로 끌어온 외부 콘텐츠(이슈 설명, 문서, 웹 콘텐츠)에 악성 지시가 섞여 들어올 때 이를 지시가 아닌 데이터로 취급한다는 런타임 방어 원칙은 AGENTS.md 어디에도 없다.

**3. AI 의사결정의 근거(provenance)가 남지 않는다**

저장소 전체에서 "adr"로 검색해 나온 유일한 결과는 `docs/analytics/adr.md`였는데, 실제로 열어보면 Cloudflare 리포터 설계 문서로 이름만 우연히 같을 뿐이다. "AI가 A안을 제안했는데 사람이 B로 결정했고 이유는 C였다"를 남기는 로그는 어디에도 없다. C3(`bd remember` 0건, 8.3절)와 같은 계열의 문제지만 각도가 다르다 — 실패 기록이 아니라 **판단 근거 기록**이 비어 있어, 다음 세션의 AI가 왜 그 결정이 났는지 매번 처음부터 다시 추론해야 한다.

**4. 비용/토큰 경제성을 추적하는 장치가 없다**

스킬 체인이 태스크 하나당 몇 콜, 몇 토큰, 얼마가 드는지 재는 장치가 전혀 없다. 스킬 개수는 계속 늘어나는 추세인데(9장 작성 시점 `.claude/skills/` 18개), 체인 전체가 실제로 비용 대비 효과적인지 증명하는 숫자가 없다.

**5. 프로덕션 신호가 하네스로 자동으로 되먹임되지 않는다**

실제 장애/버그(예: 최근 커밋의 웹훅 순서 가드 픽스)가 발생해도 그것이 golden case나 스킬 개선으로 이어지는 경로는 100% 수동이다 — 누군가 기억해서 `CHANGELOG.md`에 손으로 적어야 갱신된다. 에러 로그 → eval 갱신을 잇는 자동 피드백 루프는 없다.

**6. 멀티 하네스 교차검증이 이름만 있다**

AGENTS.md는 Codex/Claude Code/Cursor/Antigravity를 언급하고 `.agents/skills` ↔ `.claude/skills` 미러 구조까지 갖췄지만, 이 미러가 다른 벤더 하네스에서 실제로 동일하게 작동하는지 교차 검증한 흔적은 없다. 여러 도구를 지원한다는 문서만 있을 뿐, 한 벤더 종속 위험을 완화했는지는 검증되지 않았다.

**7. 체인이 처음부터 끝까지 선형으로 고정되어 있다**

`intake → planning → backend → frontend → ...`로 철저히 순차 구조다(AGENTS.md "Project Skills"). 병렬화가 안전한 지점(예: 계약이 고정된 뒤 백엔드 구현과 프론트 UI 뼈대를 동시 진행)에서도 fan-out/서브에이전트 병렬 실행을 검토한 흔적이 없다.

**8. 제품 내부 AI 기능을 뒤로 미룬 근거가 정량적이지 않다**

4.8/8.5는 이를 P3로 미뤘는데, 근거는 "개발 운영 보조 우선"이라는 원칙 선언뿐이다. 개발 프로세스 개선과 제품 내 AI 기능 중 어느 쪽이 더 큰 레버리지인지 비교한 근거는 없다. 지난 3주간 투자가 전부 내부 하네스 쪽으로 갔는데, 이 배분이 맞는지 검증된 적이 없다.

### 9.3 메모

이 목록도 결국 "조직이 AI 도입을 볼 때 일반적으로 쓰는 틀"이라는 하나의 프레임일 뿐이다 — 8장이 겪은 자기 참조 함정을 그대로 반복하지 않으려면, 이 중 무엇이 실제로 이 프로젝트 규모에 필요한지와 무엇이 과잉인지를 갈라야 한다. 예를 들어 8번(제품 AI 기능 재검토)과 1번(결과 측정)은 착수 비용이 낮고 임팩트가 크지만, 나머지는 팀·제품 규모가 커지기 전엔 우선순위가 낮을 수 있다. 실제 착수 여부와 순서는 8.5와 마찬가지로 사용자와 논의 후 별도 beads 이슈로 분리한다.

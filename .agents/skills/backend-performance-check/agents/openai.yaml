interface:
  display_name: "Dowin Backend Performance Check"
  short_description: "Dowin backend-only static performance review"
  default_prompt: "Use $dowin-backend-performance-check to statically review a Dowin backend change for aggregation/query cost risk."

policy:
  allow_implicit_invocation: true

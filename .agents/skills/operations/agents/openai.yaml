interface:
  display_name: "DOWIN Operations"
  short_description: "DOWIN incident response and runbooks"
  default_prompt: "Use $dowin-operations for DOWIN production operations work such as incident readiness, DB restore, deployment rollback, Cloudflare outage response, and operational runbooks."

policy:
  allow_implicit_invocation: true

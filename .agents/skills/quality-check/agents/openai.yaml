interface:
  display_name: "DOWIN Quality Check"
  short_description: "DOWIN verification and quality gates"
  default_prompt: "Use $dowin-quality-check to verify a DOWIN change against tests, quality gates, and regression checks before completion."

policy:
  allow_implicit_invocation: true
